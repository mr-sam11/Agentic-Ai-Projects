1. create virtual env  python3 -m venv venv
2. create requirements.txt file and put required lib and pip install -r requirements.txt
3. commiting code into git repo.
   - creating ".gitignore" file
   