1. create a folder or proejct name then create virtual env "python -m venv venv"
2. create requirements.txt file and put required lib and install "pip install -r requirements.txt" 
3. Git setup 
    - create .gitignore and README.md file 
    - create repo into github
        1 git init  : git repo has been inititalisized 
        2/